# Examples

The example files are synthetic and intentionally small. They do not copy any production Archicad export, client template, address, project name, or apartment data.

Use these examples to understand configuration shape and parser behavior. For a real project, create private project configs and keep production input/output files outside Git.
